import { Typography } from '@mui/material';
import { AppRoutes } from '../routes/app-routes';

export function App() {
  return (
    <>
      <Typography variant="h1">It is the Remote App </Typography>
      <AppRoutes />
    </>
  );
}

export default App;
